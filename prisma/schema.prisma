datasource db {
  provider = "postgresql"
  // url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

/* ---------- ENUMS ---------- */

enum KnowledgeDocumentType {
  FAQ
  DOCUMENTO
  CONTRATO
  PLAN
  TICKET
  COBRO
  PROTOCOLO
  FUNCION
  OTRO
}

enum ChatRole {
  USER
  ASSISTANT
  SYSTEM
  TOOL
}

enum ChatChannel {
  WHATSAPP
  WEB
  TELEGRAM
  SMS
  OTHER
}

enum ChatSessionStatus {
  OPEN
  CLOSED
  ARCHIVED
}


enum ChatFlowIntent {
  NONE
  SOPORTE
  VENTAS
  FACTURACION
  TIENDA
  INFORMACION
}

enum ChatFlowStep {
  NONE
  IDENTIFICACION
  DIAGNOSTICO
  CONFIRMACION
  ACCION
  CIERRE
}


/* ---------- EMPRESA / CLIENTE ---------- */

model Empresa {
  id            Int              @id @default(autoincrement())
  nombre        String
  slug          String           @unique   // "nova-sistemas", etc.
  activo        Boolean          @default(true)

  creadoEn      DateTime         @default(now())
  actualizadoEn DateTime         @updatedAt

  clientes      Cliente[]
  documentos    KnowledgeDocument[]
  sesiones      ChatSession[]
  bots          Bot[]           
}

enum BotStatus {
  ACTIVE
  DISABLED
}

model Bot {
  id           Int       @id @default(autoincrement())

  empresa      Empresa   @relation(fields: [empresaId], references: [id], onDelete: Cascade)
  empresaId    Int

  nombre       String
  slug         String    @unique
  descripcion  String?

  provider     String    @default("fireworks")
  model        String    @default("accounts/fireworks/models/gpt-oss-120b")

  systemPrompt   String   @db.Text

  contextPrompt  String?  @db.Text 
  historyPrompt  String?  @db.Text 

  outputStyle    String?  @db.Text

  maxCompletionTokens Int?    @default(500)
  temperature         Float   @default(0.7)
  topP                Float?  @default(0.9)
  frequencyPenalty    Float?  @default(0.2)
  presencePenalty     Float?  @default(0.0)

  maxHistoryMessages  Int?    @default(15)
  status        BotStatus @default(ACTIVE)

  creadoEn      DateTime  @default(now())
  actualizadoEn DateTime  @updatedAt

  sesiones       ChatSession[]
}


model Cliente {
  id            Int       @id @default(autoincrement())

  empresa       Empresa   @relation(fields: [empresaId], references: [id], onDelete: Cascade)
  empresaId     Int

  nombre        String?
  telefono      String         
  uuid          String? @unique 
  crmUsuarioId  Int?

  creadoEn      DateTime  @default(now())
  actualizadoEn DateTime  @updatedAt
  
  botActivo     Boolean   @default(true) 

  sesiones      ChatSession[]

  ultimoMensajeFecha DateTime @default(now())

  whatsappHistory WhatsappMessage[]
  @@unique([empresaId, telefono])
}

/* ---------- CHAT ---------- */

model ChatSession {
  id                   Int               @id @default(autoincrement())

  empresa              Empresa           @relation(fields: [empresaId], references: [id], onDelete: Cascade)
  empresaId            Int

  bot                  Bot?              @relation(fields: [botId], references: [id])
  botId                Int?

  cliente              Cliente?          @relation(fields: [clienteId], references: [id])
  clienteId            Int?

  telefono             String
  canal                ChatChannel
  estado               ChatSessionStatus @default(OPEN)

  whatsappMessages WhatsappMessage[]

  ultimoTicketCrmId    String?
  ultimoTicketCreadoEn DateTime?

  iniciadoEn           DateTime          @default(now())
  cerradoEn            DateTime?
  creadoEn             DateTime          @default(now())
  actualizadoEn        DateTime          @updatedAt

  mensajes             ChatMessage[]

  flowIntent   ChatFlowIntent @default(NONE)
  flowStep     ChatFlowStep   @default(NONE)
  flowContext  Json?


  openaiLastResponseId String? @db.VarChar(255)

  @@index([empresaId, telefono, canal, estado])
}

model ChatMessage {
  id         Int        @id @default(autoincrement())

  session    ChatSession @relation(fields: [sessionId], references: [id], onDelete: Cascade)
  sessionId  Int

  rol        ChatRole
  contenido  String
  tokens     Int?

  mediaUrl   String?

  creadoEn   DateTime    @default(now())
  actualizadoEn DateTime         @updatedAt

  @@index([sessionId, creadoEn])
}

/* ---------- RAG: DOCUMENTOS / CHUNKS ---------- */

model KnowledgeDocument {
  id          Int                   @id @default(autoincrement())

  empresa     Empresa               @relation(fields: [empresaId], references: [id], onDelete: Cascade)
  empresaId   Int

  tipo        KnowledgeDocumentType
  externoId   Int?
  origen      String?

  titulo      String
  descripcion String?
  textoLargo String? @db.Text

  creadoEn      DateTime            @default(now())
  actualizadoEn DateTime            @updatedAt

  chunks      KnowledgeChunk[]

  activo      Boolean @default(true) 

  @@index([empresaId, tipo])
}

model KnowledgeChunk {
  id         Int               @id @default(autoincrement())

  document   KnowledgeDocument @relation(fields: [documentId], references: [id], onDelete: Cascade)
  documentId Int

  indice     Int
  texto      String

  embedding  String?
  tokens     Int?

  creadoEn       DateTime @default(now())
  actualizadoEn  DateTime @updatedAt

  @@index([documentId, indice])
}

/* ---------- RAG: DOCUMENTOS / CHUNKS ---------- */


/* ---------- WHATSAPPP: PERSISTENCIA DE CHATS ---------- */
model WhatsappMessage {
  id        Int   @id @default(autoincrement()) // ID interno nuestro
  
  wamid     String   @unique 
  
  chatSessionId Int?
  chatSession   ChatSession? @relation(fields: [chatSessionId], references: [id])
  
  clienteId     Int?
  cliente       Cliente?     @relation(fields: [clienteId], references: [id])

  direction     WazDirection
  from          String      
  to            String       
  
  type          WazMediaType
  body          String?      @db.Text 
  
  mediaUrl      String?      // URL pública del archivo
  mediaMimeType String?      // image/jpeg, audio/ogg
  mediaSha256   String?      // Para evitar re-subir el mismo archivo
  
  status        WazStatus    @default(SENT)
  errorCode     String?      
  errorMessage  String?

  replyToWamid  String?      
  
  timestamp     BigInt       // Timestamp original de WhatsApp (Unix)
  creadoEn      DateTime     @default(now())
  actualizadoEn DateTime     @updatedAt

  @@index([wamid])
  @@index([clienteId])
  @@index([status])
}


// ENUMS
// Dirección del mensaje: ¿Entra al servidor o Sale del servidor?
enum WazDirection {
  INBOUND  // Cliente -> Bot
  OUTBOUND // Bot -> Cliente
}

// Estado de entrega (El "Check")
enum WazStatus {
  SENT      // Un check gris (Enviado a Meta)
  DELIVERED // Doble check gris (Llegó al celular)
  READ      // Doble check azul (Leído)
  FAILED    // Error (No salió)
}

// Tipo de contenido (Para saber cómo renderizarlo en el UI)
enum WazMediaType {
  TEXT
  IMAGE
  AUDIO     // Notas de voz
  VIDEO
  DOCUMENT  // PDFs
  STICKER
  LOCATION
  TEMPLATE  // Plantillas de marketing
  INTERACTIVE // Botones y listas
  UNKNOWN
}






